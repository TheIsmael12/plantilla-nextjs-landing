import ProfileInfoViewPage from '@/views/(client-area)/private-area/profile/info/ProfileInfoViewPage';

/**
 * Página de perfil del portal de cliente: datos personales de solo lectura.
 * @returns {JSX.Element} La vista de datos personales renderizada
 */
export default function ProfilePage() {
    return <ProfileInfoViewPage />;
}
