import PrivateAreaHomeViewPage from '@/views/(client-area)/private-area/PrivateAreaHomeViewPage';

/**
 * Página de `/private-area`: home del área privada del cliente, con acceso
 * a perfil, servicios contratados, presupuestos y facturas.
 * @returns {JSX.Element} La página de inicio del área privada renderizada
 */
export default function PrivateAreaHomePage() {
    return <PrivateAreaHomeViewPage />;
}
