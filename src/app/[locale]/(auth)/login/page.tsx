import LoginViewPage from '@/views/(auth)/login/LoginViewPage';

/**
 * Página de login del portal de cliente.
 * @returns {JSX.Element} La vista de login renderizada
 */
export default function LoginPage() {
    return <LoginViewPage />;
}
