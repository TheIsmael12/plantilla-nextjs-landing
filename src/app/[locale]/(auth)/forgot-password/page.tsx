import ForgotPasswordViewPage from '@/views/(auth)/forgot-password/ForgotPasswordViewPage';

/**
 * Página de recuperación de acceso del portal de cliente.
 * @returns {JSX.Element} La vista de recuperación de acceso renderizada
 */
export default function ForgotPasswordPage() {
    return <ForgotPasswordViewPage />;
}
