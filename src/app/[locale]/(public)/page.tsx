import HomeViewPage from '@/views/(public)/home/HomeViewPage';

/**
 * Página de inicio del sitio público.
 * @returns {JSX.Element} La vista de inicio renderizada
 */
export default function HomePage() {
    return <HomeViewPage />;
}