import AboutViewPage from '@/views/(public)/about/AboutViewPage';

/**
 * Página "Sobre nosotros" del sitio público.
 * @returns {JSX.Element} La vista de la página About renderizada
 */
export default function AboutPage() {
    return <AboutViewPage />;
}
