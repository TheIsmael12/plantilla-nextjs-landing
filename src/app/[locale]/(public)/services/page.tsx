import ServicesViewPage from '@/views/(public)/services/ServicesViewPage';

/**
 * Página de servicios del sitio público.
 * @returns {JSX.Element} La vista de servicios renderizada
 */
export default function ServicesPage() {
    return <ServicesViewPage />;
}
