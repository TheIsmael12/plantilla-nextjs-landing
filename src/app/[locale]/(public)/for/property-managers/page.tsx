import PropertyManagersViewPage from '@/views/(public)/for/PropertyManagersViewPage';

/**
 * Página "Servicios para administradores de fincas" del sitio público.
 * @returns {JSX.Element} La vista renderizada
 */
export default function PropertyManagersPage() {
    return <PropertyManagersViewPage />;
}
