import CookiesView from '@/views/(public)/terms/CookiesView';

/**
 * Página de la política de cookies del sitio.
 * @returns {JSX.Element} La vista de política de cookies renderizada
 */
export default function CookiesPolicyPage() {
    return <CookiesView />;
}
