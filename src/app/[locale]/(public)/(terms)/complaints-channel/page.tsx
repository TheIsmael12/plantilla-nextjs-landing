import ComplaintsChannelView from '@/views/(public)/terms/ComplaintsChannelView';

/**
 * Página del canal de reclamaciones del sitio público.
 * @returns {JSX.Element} La vista del canal de reclamaciones renderizada
 */
export default function ComplaintsChannelPage() {
    return <ComplaintsChannelView />;
}
