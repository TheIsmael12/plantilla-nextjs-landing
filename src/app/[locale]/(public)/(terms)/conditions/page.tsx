import TermsView from '@/views/(public)/terms/TermsView';

/**
 * Página de términos y condiciones del sitio.
 * @returns {JSX.Element} La vista de términos y condiciones renderizada
 */
export default function ConditionsPage() {
    return <TermsView />;
}
