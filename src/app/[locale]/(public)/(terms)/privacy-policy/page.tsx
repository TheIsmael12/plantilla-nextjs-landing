import PrivacyView from '@/views/(public)/terms/PrivacyView';

/**
 * Página de la política de privacidad del sitio.
 * @returns {JSX.Element} La vista de política de privacidad renderizada
 */
export default function PrivacyPolicyPage() {
    return <PrivacyView />;
}
