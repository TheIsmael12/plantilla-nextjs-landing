import ContactViewPage from '@/views/(public)/contact/ContactViewPage';

/**
 * Página de contacto del sitio público.
 * @returns {JSX.Element} La vista de contacto renderizada
 */
export default function ContactPage() {
    return <ContactViewPage />;
}