import ComplaintsHelpViewPage from '@/views/(public)/help/complaints/ComplaintsHelpViewPage';

/**
 * Página del canal de reclamaciones dentro del centro de ayuda: el asistente por pasos.
 * @returns {JSX.Element} La vista renderizada
 */
export default function HelpComplaintsPage() {
    return <ComplaintsHelpViewPage />;
}
