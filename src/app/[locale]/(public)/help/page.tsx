import HelpViewPage from '@/views/(public)/help/HelpViewPage';

/**
 * Página de ayuda del sitio público.
 * @returns {JSX.Element} La vista de ayuda renderizada
 */
export default function HelpPage() {
    return <HelpViewPage />;
}
