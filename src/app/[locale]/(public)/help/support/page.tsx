import SupportViewPage from '@/views/(public)/support/SupportViewPage';

/**
 * Página de soporte del sitio público.
 * @returns {JSX.Element} La vista de soporte renderizada
 */
export default function SupportPage() {
    return <SupportViewPage />;
}
